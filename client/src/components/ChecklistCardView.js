import React, { Component } from 'react';
import ChecklistCard from "./ChecklistCard";

const ChecklistCardView = (props) => {
  return (
    <div>

    </div>
  );
};

export default ChecklistCardView;